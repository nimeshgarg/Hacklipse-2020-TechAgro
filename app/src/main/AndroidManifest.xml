<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techargo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TechArgo">
        <activity android:name=".AreaInfo"></activity>
        <activity android:name=".PestAttack" />
        <activity android:name=".AddCrop" />
        <activity android:name=".Register" />
        <activity android:name=".MyArea" />
        <activity android:name=".MyField" />
        <activity android:name=".MyProfile" />
        <activity android:name=".MainPage" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>


</manifest>